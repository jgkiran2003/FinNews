# ── CORE APPLICATIONS & DASHBOARD ───────────────────────────────────────────
streamlit>=1.35.0
yfinance>=0.2.40
plotly>=5.20.0
requests>=2.32.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
newsapi-python>=0.2.7

# ── DATA SCIENCE & MACHINE LEARNING ─────────────────────────────────────────
# Python 3.10/3.11/3.12 compatible core stack
numpy>=1.26.0,<2.0.0
pandas>=2.1.0
scipy>=1.11.0
scikit-learn>=1.4.0

# Deep Learning Frameworks (Pinned loosely to allow platform-specific wheels)
torch>=2.2.0
tensorflow>=2.16.0
transformers>=4.40.0
accelerate>=0.30.0
datasets>=2.19.0
safetensors>=0.4.0
tokenizers>=0.19.0

# ── HARDWARE & ACCELERATION OPTIONAL OVERRIDES ──────────────────────────────
# Pinned loosely so pip can safely bypass if hardware-specific binaries are missing
mkl-service>=2.4.0
mkl_fft>=1.3.8
mkl_random>=1.2.4
